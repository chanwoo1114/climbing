# 호스트에 노출할 dev 서버 포트 (공유기 내부 포트와 맞출 것)
FRONT_PORT=5180

# 브라우저는 상대경로(/api, /ws)만 쓰고 vite가 프록시한다.
# 아래 값들은 특별한 경우에만 채운다.
#VITE_API_BASE_URL=      # 프록시를 안 거치고 API를 직접 칠 때만
#VITE_WS_BASE_URL=       # WebSocket을 직접 연결할 때만
#VITE_PROXY_TARGET=      # 프록시가 바라볼 백엔드 주소 (기본 http://localhost:8010)
